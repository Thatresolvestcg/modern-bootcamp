const age = 31;
const dogYears = (age + 1) / 7;
console.log(Math.floor(dogYears));

// challenge area 
const studentScore = 18;
const maxScore = 100;
const percent = (studentScore / maxScore) * 100;
console.log(percent);